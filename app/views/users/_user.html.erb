<div class="p-4 w-full" id="<%= dom_id user %>">
  <div class="flex rounded-lg h-full bg-gray-100 p-8 flex-col md:w-1/3 mx-auto">
    <div class="flex-grow">
      <h2 class="text-gray-900 text-lg title-font font-medium"><%= user.name %></h2>
      <a class="mt-3 text-indigo-500 inline-flex items-center">
        <%= link_to "Show this user", user %>
      </a>
    </div>
  </div>
</div>